
version: 2

sources:
    - name: staging

      # For postgres:
      database: ny-rides-evan

      schema: avm_data

      tables:
        - name: avm_data_table_test
         # freshness:
           # error_after: {count: 6, period: hour}

models:
    - name: stg_avm_data

      description: >
        Table contains merged timeseries data from historical zillow data and american community survey data